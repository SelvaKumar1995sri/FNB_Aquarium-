services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fnbaqua
      POSTGRES_USER: fnbaqua
      POSTGRES_PASSWORD: fnbaqua
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
